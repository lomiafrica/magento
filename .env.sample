# Environment variables for local development only.
# Copy dev/.env.example to dev/.env — see dev/README.md.

# Production stores configure keys in:
# Stores → Configuration → Sales → Payment Methods → lomi.
